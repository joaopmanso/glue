---
date: 2026-09-26
---
# Handoff (2), 2026-09-26: the user's batch after Prepare step 1

If the session ends midway, carry on from here. State at writing: `main` = `87f5ec3` (deployed). Work
happens on the `home-engine` branch and is fast-forwarded to `main`.

## Standing rules from the user
- Deploy after validation: fast-forward `main` and push (the site deploys); release GLUE Home (a tag)
  only when `home/` changes.
- Keep the vault current in the same commit as the code.

## The batch (user, 2026-09-26), in order
1. **BPM the same everywhere.**
   - The Details page's Tempo card (MusicCard) and its "GLUE analysis" row show the raw analysis
     (114.7, ½×, 2×). They should follow the correction and the profile range.
   - The user's "two songs" (Mala - Changes…): **two files** in Music Collection, `….aiff` and
     `…_playable_v2.aiff` (20 bytes apart, same audio). The correction was on one only.
   - Plan: a Prepare BPM correction and flip apply to every copy in the same "same recording" group
     (Duplicates); the grid only when the lengths match.
   - Also seen: the analysis gave 114.67 (⅔ of 172, a triplet error) while Traktor (86) and Engine (172)
     agree. Idea: flag tracks where the analysis disagrees with the DJ apps.
2. **An outline on the playing song** in the library table.
3. **Drag and drop into Rekordbox / Engine DJ** (songs and playlists), from anywhere on a row, with a
   "dragging N tracks" icon; playlists draggable to reorganise, onto folders, and out to the apps.
   - Dropping into Explorer works (DownloadURL, ADR 0027); into the DJ apps it doesn't.
   - Needs research first: what those apps accept (real file paths, `text/uri-list` file URLs, M3U
     files?). In Home mode the site knows absolute paths (`root.absPath`), and GLUE Home could start
     a native drag. **[UNVERIFIED]** until tried with the apps on the desktop.
4. **Prepare step 2:** cue points (hot A–H, memory) and loops (ADR 0052, `features/prepare.md`).

## Progress
- Done and deployed (see the changelog):
  - 1. BPM everywhere, and copies of the same recording;
  - 2. the playing song's outline, and "Show in list" (⌖) in the mini player;
  - the cloud clean-up race.
- 3. Drag and drop: Rekordbox 7.2.13 and Engine DJ 5.1 are installed on the desktop. Testing drops into
  them needs the user's OK: it takes over the mouse, and adds a test track to their libraries. Asked
  on 2026-09-26.
- 4. Cues: next.

## Open issues noted
- The e2e cloud-sync test once left one file in the cloud after "clean up" (features/glue-cloud.md).
- Debug builds of GLUE Home crashed in browser tests; release builds didn't (features/glue-cloud.md).
